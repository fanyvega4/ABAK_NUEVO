@{
    ViewData["Title"] = "Acceso denegado";
    Layout = "_Layout"; // o el layout que estés usando
}

<section class="about-us" style="min-height:60vh; display:flex; align-items:center; justify-content:center; text-align:center;">
    <div>
        <h1>Acceso denegado</h1>
        <p>No tienes permisos para acceder a esta sección.</p>
        <a asp-controller="Home" asp-action="Index" class="btn btn__primary">
            Volver al inicio
        </a>
    </div>
</section>
